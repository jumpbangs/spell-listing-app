import { createTheme } from '@mui/material';

export const appTheme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#244e6e',
    },
    secondary: {
      main: '#52606e',
    },
    background: {
      default: '#424242',
    },
  },
});
